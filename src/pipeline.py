import datasets


def tokenize():
    return ...

def main():
    dataset = datasets.Dataset = ...
    dataset = dataset.map(
        tokenize()
    )

    trainer = Trainer(

    )

    model = trainer.model